# Markdown 速查（写笔记专用）

> 这份文件本身就是示范：下面每段「源码」后面，就是它渲染出来的样子。

## 一、标题：用 # 的数量表示层级

```
# 一级标题（每篇笔记只有一个，写章节名）
## 二级标题（一节内容一个）
### 三级标题（细分小点）
```

> ⚠️ `#` 后面必须有一个空格，否则不生效。

## 二、强调与行内代码

```
**粗体**        *斜体*        ~~删除线~~        `行内代码`
```

**粗体**　*斜体*　~~删除线~~　`int a[10]`

> 行内代码用反引号（键盘左上角 Esc 下面那个键），写变量名、函数名特别合适。

## 三、要点列表

```
- 无序要点
- 第二点
  - 往右缩进两个空格，就是子要点
    - 再缩进，就是孙要点

1. 有序步骤
2. 第二步
```

渲染效果：

- 无序要点
- 第二点
  - 缩进两个空格就是子要点

## 四、待办清单（复习计划特别好用）

```
- [ ] 还没做的事
- [x] 已完成的事
```

- [ ] 复习数组的三种遍历
- [x] 完成数组笔记

## 五、表格：算法笔记的刚需（复杂度表）

```
| 操作 | 时间复杂度 | 说明 |
|------|-----------|------|
| 按索引访问 | O(1) | 直接算地址 |
| 中间插入 | O(n) | 后面元素整体后移 |
```

渲染效果：

| 操作 | 时间复杂度 | 说明 |
|------|-----------|------|
| 按索引访问 | O(1) | 直接算地址 |
| 中间插入 | O(n) | 后面元素整体后移 |

> 技巧：第二行的 `|------|` 是分隔行，必须有；冒号控制对齐，`:---` 左对齐、`:---:` 居中、`---:` 右对齐。

## 六、代码块：写代码必须有

```
```cpp
int main() {
    cout << "hello" << endl;
    return 0;
}
```
```

渲染效果：

```cpp
int main() {
    cout << "hello" << endl;
    return 0;
}
```

> 开头的 ``` 后面写上语言名（cpp / python / bash），代码就会有颜色高亮。

## 七、引用与提示块

```
> 普通引用，用来放老师原话、结论

> [!tip] 技巧提示
> 数组越界不会报错，但结果会诡异，要自己盯下标

> [!warning] 踩坑警告
> `newNode->next = p->next` 必须在 `p->next = newNode` 之前
```

渲染效果：

> [!tip] 技巧提示
> 数组越界不会报错，但结果会诡异，要自己盯下标

> [!warning] 踩坑警告
> `newNode->next = p->next` 必须在 `p->next = newNode` 之前

### 五种提示块的渲染效果

| 类型 | 颜色与图标 | 适合写什么 |
|------|-----------|-----------|
| `[!note]` | 蓝色 · 铅笔 | 补充说明、前提条件 |
| `[!tip]` | 青绿色 · 火苗 | 技巧、省事的小方法 |
| `[!warning]` | 橙黄色 · 警告 | 易错点、踩坑警告 |
| `[!question]` | 紫色 · 问号 | 还没搞懂的疑问、待查证 |
| `[!example]` | 灰紫色 · 列表 | 例题、代码示例 |

下面是五种真实渲染效果（点开阅读模式 `Ctrl+E` 效果最明显）：

> [!note] 笔记
> 数组下标从 0 开始，第 i 个元素地址 = 首地址 + i × 元素大小

> [!tip] 技巧提示
> 数组越界不会报错，但结果会诡异，要自己盯紧下标

> [!warning] 踩坑警告
> （待填：今天在 VS 里遇到的错误）

> [!question] 还没搞懂
> 为什么动态数组扩容要按 2 倍而不是每次 +1？

> [!example] 例题
> 反转一个长度为 n 的数组，要求空间复杂度 O(1)

## 八、数学公式（写复杂度必备）

Obsidian 内置公式渲染，**开箱即用**，不用装插件。规则：**行内公式用一对 `$` 包起来，块级公式用一对 `$$` 单独成行**。

### 最常用的复杂度写法

| 你想要的 | 写法 | 渲染效果 |
|---------|------|---------|
| 常数阶 | `$O(1)$` | $O(1)$ |
| 对数阶 | `$O(\log n)$` | $O(\log n)$ |
| 线性阶 | `$O(n)$` | $O(n)$ |
| 线性对数阶 | `$O(n \log n)$` | $O(n \log n)$ |
| 平方阶 | `$O(n^2)$` | $O(n^2)$ |
| 立方阶 | `$O(n^3)$` | $O(n^3)$ |
| 指数阶 | `$O(2^n)$` | $O(2^n)$ |
| 阶乘阶 | `$O(n!)$` | $O(n!)$ |
| 平均情况 | `$\Theta(n)$` | $\Theta(n)$ |
| 下界 | `$\Omega(n)$` | $\Omega(n)$ |

### 容易写错的地方

| 错误写法 | 问题 | 正确写法 |
|---------|------|---------|
| `$O(log n)$` | 会渲染成斜体字母 l·o·g | `$O(\log n)$` |
| `$O(n log n)$` | 同上，n 和 log 粘在一起 | `$O(n \log n)$` |
| `$O(n^2)$` 写成 `O(n2)` | 2 不会变成上标 | 用 `^` 表示上标 |
| `$O(n^3)$` 写成 `$O(n^3 )$` | 一般没事，但空格别乱塞 | 保持紧凑 |

### 求和、分式、递推式（块级公式）

```
$$
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}
$$
```

$$
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}
$$

```
$$
T(n) = 2T\left(\frac{n}{2}\right) + O(n) = O(n \log n)
$$
```

$$
T(n) = 2T\left(\frac{n}{2}\right) + O(n) = O(n \log n)
$$

### 大 O 的数学定义（想严谨时用）

```
$$
f(n) = O(g(n)) \iff \exists c > 0,\ n_0 > 0,\ \forall n \ge n_0:\ f(n) \le c \cdot g(n)
$$
```

$$
f(n) = O(g(n)) \iff \exists c > 0,\ n_0 > 0,\ \forall n \ge n_0:\ f(n) \le c \cdot g(n)
$$

### 符号速查

| 符号 | 写法 | 符号 | 写法 |
|------|------|------|------|
| 求和 | `\sum` | 根号 | `\sqrt{n}` |
| 分式 | `\frac{a}{b}` | 无穷 | `\infty` |
| 小于等于 | `\le` | 大于等于 | `\ge` |
| 约等于 | `\approx` | 乘号 | `\cdot` |
| 换行 | `\\` | 空格 | `\ ` 或 `\;` |

> 记住一条就够用：**所有多字母的运算符前面都要加反斜杠**（`\log`、`\sin`、`\lim`），单个变量不用加。

## 九、图片：可以直接复制粘贴

**可以直接 `Ctrl+V` 粘贴**，这是 Obsidian 最爽的功能之一：

1. 截图（`Win+Shift+S` 框选）→ 回到 Obsidian 笔记 → `Ctrl+V`
2. Obsidian 自动把图片存进仓库，并在光标处插入一行图片引用
3. 也可以拖拽图片文件进来，效果一样

### 一定要改的附件设置（避免图片乱丢）

设置 → **文件与链接** → 「附件默认存放路径」→ 选 **「指定的附件文件夹」** → 填 `_附件`

这样所有粘贴的图片都集中到 `_附件/` 文件夹，仓库不会乱七八糟。

### ⚠️ 想让图片在 GitHub 上也显示，必须注意这点

Obsidian 默认用**双链语法**插入图片：`![[Pasted image 20260914163000.png]]`
这种写法**只在 Obsidian 里能看，GitHub 网页上会显示不出来**。

解决办法：设置 → 文件与链接 → **关闭「使用 Wiki 链接」**（改成 `[链接](路径)` 格式）。

关掉之后插入的图片是标准 Markdown 语法，GitHub 上也能正常显示：

```
![图片说明](_附件/图1.png)
```

### 图片使用建议

- 算法笔记最好用图片的地方：**数据结构示意图、老师板书截图、手画的指针变化过程**
- 截图足够清晰就行，别存几 MB 的大图（仓库会变臃肿）
- 常用格式：`.png`（截图）、`.svg`（示意图）

## 十、链接与分隔线

```
[链接文字](https://www.bilibili.com)

---
```

`---` 单独一行就是一条分隔线（下面这条横线）：

---

## 十一、Obsidian 专属：双链与标签

### 双链 `[[ ]]`

```
[[01-线性结构/1.1-顺序表]]          ← 链接到某篇笔记
[[01-线性结构/1.1-顺序表|数组笔记]]   ← 显示成自定义文字
```

**好处：**

1. **一键跳转**：`Ctrl+点击` 直接跳到那篇笔记，不用在文件夹里翻
2. **反向链接**：打开某篇笔记，右侧面板会列出「**哪些笔记引用了它**」——比如打开「1.1-顺序表」，能看到其他笔记里提到过它，知识就自动连成网了
3. **自动补全**：输入 `[[` 就会弹出笔记列表，不用记路径

**学习中的用法**：写链表笔记时提一句「对比 [[01-线性结构/1.1-顺序表]] 的随机访问优势」，以后复习链表会自动看到这个关联点。

### 标签 `#`

```
#数组  #待复习  #易错点
```

**好处：**

1. **跨文件夹聚合**：点一下 `#待复习`，所有打了这个标签的笔记全部列出来，复习时不用挨个翻章节
2. **配合搜索**：`Ctrl+Shift+F` 搜 `tag:#待复习` 精确筛选
3. **可放正文任意位置**，通常写在笔记开头或某段末尾

**使用建议**：标签要少而稳定，建议只用几个固定的，比如：

| 标签 | 什么时候打 |
|------|-----------|
| `#待复习` | 写得不够熟、需要再看 |
| `#易错点` | 自己踩过坑的地方 |
| `#没搞懂` | 存疑、下课要问老师 |
| `#重点` | 老师强调必考的 |

## 十二、换行与分段的规则

- **空一行** = 新起一段（最常用）
- 想在同一段里强制换行：行尾敲**两个空格**再回车（不推荐，容易乱）
- 不空行直接回车，Obsidian 会当成同一段

## 十三、写算法笔记的推荐结构

```markdown
# 02 链表

#重点 #待复习

## 是什么
一句话：用指针把零散节点串起来的线性结构

## 核心操作与复杂度
| 操作 | 复杂度 |

## 代码要点
关键几行的解释

## 踩坑记录
> [!warning] 这里放自己犯的错

## 自测题
1. 能自己回答出来的问题
```

> 建好结构后，每周复习就是读自己的笔记，而不是重看两小时视频。
