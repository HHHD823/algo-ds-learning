# Markdown 速查（写笔记专用）

> 这份文件本身就是示范：下面每段「源码」后面，就是它渲染出来的样子。

## 一、标题：用 # 的数量表示层级

```
# 一级标题（每篇笔记只有一个，写章节名）
## 二级标题（一节内容一个）
### 三级标题（细分小点）
```

> ⚠️ `#` 后面必须有一个空格，否则不生效。

## 二、强调与行内代码

```
**粗体**        *斜体*        ~~删除线~~        `行内代码`
```

**粗体**　*斜体*　~~删除线~~　`int a[10]`

> 行内代码用反引号（键盘左上角 Esc 下面那个键），写变量名、函数名特别合适。

## 三、要点列表

```
- 无序要点
- 第二点
  - 往右缩进两个空格，就是子要点
    - 再缩进，就是孙要点

1. 有序步骤
2. 第二步
```

渲染效果：

- 无序要点
- 第二点
  - 缩进两个空格就是子要点

## 四、待办清单（复习计划特别好用）

```
- [ ] 还没做的事
- [x] 已完成的事
```

- [ ] 复习数组的三种遍历
- [x] 完成数组笔记

## 五、表格：算法笔记的刚需（复杂度表）

```
| 操作 | 时间复杂度 | 说明 |
|------|-----------|------|
| 按索引访问 | O(1) | 直接算地址 |
| 中间插入 | O(n) | 后面元素整体后移 |
```

渲染效果：

| 操作 | 时间复杂度 | 说明 |
|------|-----------|------|
| 按索引访问 | O(1) | 直接算地址 |
| 中间插入 | O(n) | 后面元素整体后移 |

> 技巧：第二行的 `|------|` 是分隔行，必须有；冒号控制对齐，`:---` 左对齐、`:---:` 居中、`---:` 右对齐。

## 六、代码块：写代码必须有

```
```cpp
int main() {
    cout << "hello" << endl;
    return 0;
}
```
```

渲染效果：

```cpp
int main() {
    cout << "hello" << endl;
    return 0;
}
```

> 开头的 ``` 后面写上语言名（cpp / python / bash），代码就会有颜色高亮。

## 七、引用与提示块

```
> 普通引用，用来放老师原话、结论

> [!tip] 技巧提示
> 数组越界不会报错，但结果会诡异，要自己盯下标

> [!warning] 踩坑警告
> `newNode->next = p->next` 必须在 `p->next = newNode` 之前
```

渲染效果：

> [!tip] 技巧提示
> 数组越界不会报错，但结果会诡异，要自己盯下标

> [!warning] 踩坑警告
> `newNode->next = p->next` 必须在 `p->next = newNode` 之前

> 常用类型：`[!note]` `[!tip]` `[!warning]` `[!question]` `[!example]`

## 八、数学公式（写复杂度用得上）

```
行内公式：$O(n \log n)$

块级公式：
$$
T(n) = 2T(n/2) + O(n)
$$
```

行内公式：$O(n \log n)$

## 九、链接、图片与分隔线

```
[链接文字](https://www.bilibili.com)

![图片说明](图片路径或网址)

---
```

`---` 单独一行就是一条分隔线（上面这条横线就是）：

---

## 十、Obsidian 专属：双链与标签

```
[[01-数组/notes]]        ← 链接到另一篇笔记
#数组 #待复习              ← 标签，写哪儿都行
```

> 双链的好处：以后打开「01-数组」，右侧会显示「哪些笔记引用了它」。

## 十一、换行与分段的规则

- **空一行** = 新起一段（最常用）
- 想在同一段里强制换行：行尾敲**两个空格**再回车（不推荐，容易乱）
- 不空行直接回车，Obsidian 会当成同一段

## 十二、写算法笔记的推荐结构

```markdown
# 02 链表

## 是什么
一句话：用指针把零散节点串起来的线性结构

## 核心操作与复杂度
| 操作 | 复杂度 |

## 代码要点
关键几行的解释

## 踩坑记录
> [!warning] 这里放自己犯的错

## 自测题
1. 能自己回答出来的问题
```

> 建好结构后，每周复习就是读自己的笔记，而不是重看两小时视频。
