# 学习手册（操作速查）

> 忘了怎么操作就回来翻这一页。在 Obsidian 左侧文件列表里点开即可。

## 一、三个软件的分工

| 软件 | 什么时候用 | 主要干什么 |
|------|-----------|-----------|
| Visual Studio | 听课跟练 | 敲代码、按 F5 编译运行、断点调试 |
| Obsidian | 听课记笔记 | 编辑 `notes.md`、手动提交推送 |
| VS Code | 课下练习 | 写练习代码、改文件、手动提交推送 |

三个软件打开的是**同一个文件夹** `D:\algo-ds-learning`，文件只有一份，不存在「同步」问题。

---

## 二、Obsidian 使用要点

### 基本界面
- **左侧**：文件列表（就是仓库目录，点文件夹图标展开）
- **中间**：编辑区，直接点开 `notes.md` 就能写
- **右上角**：编辑 / 阅读模式切换，快捷键 `Ctrl+E`

### 必会快捷键

| 快捷键 | 作用 |
|--------|------|
| `Ctrl+E` | 在「编辑」和「阅读」模式之间切换 |
| `Ctrl+N` | 新建笔记 |
| `Ctrl+P` | 命令面板（调用所有功能，最重要） |
| `Ctrl+Shift+F` | 全仓库搜索（找「复杂度」这种词特别好用） |
| `[[` | 输入双方括号，链接到另一篇笔记 |
| `Ctrl+点击` | 跳转到链接指向的笔记 |

### 用模板新建章节笔记（一次性设置）

1. 设置（左下角齿轮）→ **核心插件** → 找到「模板」→ 开启
2. 设置 → 左侧「模板」选项卡 → **模板文件夹位置** 填 `_模板`
3. 以后新建笔记后，按 `Ctrl+P` → 输入「插入模板」→ 选 **章节笔记模板**

### 手动提交推送（不用等自动）

| 命令（`Ctrl+P` 后输入） | 作用 |
|------------------------|------|
| `Git: Commit` | 只在本地存档一次 |
| `Git: Push` | 推送到 GitHub |
| `Git: Commit and push` | 一步到位（最常用） |
| `Git: Pull` | 从 GitHub 拉取最新（在别处改过代码后用） |

> ⚠️ 提交前确认右下角有同步提示、没有报错。若报错「no changes」，说明没有改动需要提交。

---

## 三、在 Visual Studio 里新建章节代码（推荐做法）

### 做法 A：单文件模式（最简单，推荐新手）

不用建项目，仓库干净，目录结构和笔记并排：

1. 先在资源管理器（或 Obsidian 左侧）新建章节文件夹，如 `02-链表`
2. 打开 VS → 菜单 **文件 → 打开 → 文件** → 选中 `D:\algo-ds-learning\02-链表\linked_list.cpp`
   - 如果文件还不存在，先在文件夹里新建一个空的 `.txt` 再改名成 `.cpp`，或者用 VS Code 新建
3. 写代码 → 按 `Ctrl+F5` 直接编译运行（VS 会自动处理编译配置）
4. 保存后文件就落在仓库里了

**文件名建议用英文**（`linked_list.cpp`、`array_basic.cpp`），中文文件名在命令行工具里容易出编码问题。

### 做法 B：项目模式（代码需要多个文件时）

1. VS → **创建新项目** → 选「空项目」→ 下一步
2. **项目名称**：英文名，如 `LinkedList`
3. **位置**：`D:\algo-ds-learning\02-链表`
4. ✅ 勾选 **「将解决方案和项目放在同一目录中」**（很关键，否则会多一层套娃目录）
5. 创建后右键「源文件」→ 添加 → 新建项 → C++ 文件
6. 编译产物（`x64/`、`Debug/`、`.vs/`）会被 `.gitignore` 自动忽略，不会传上 GitHub

---

## 四、在 VS Code 里课下练习

1. 左侧「资源管理器」→ 在目标章节文件夹上**右键 → 新建文件**，命名如 `array_practice.cpp`
2. 写代码，`Ctrl+S` 保存
3. 想运行调试：目前最顺的方式是**用 VS 打开同一个 `.cpp` 文件按 Ctrl+F5**（VS Code 直接编译 C++ 需要额外配置编译器，暂时不必折腾）
4. 提交推送：左侧第三个图标 **源代码管理** → 在输入框写提交信息 → 点 **✓ 提交** → 点 **同步更改**

---

## 五、提交信息怎么写

好习惯：一句话说清「完成了什么」，中文即可。

- ✅ `完成单链表的增删改查`
- ✅ `修复冒泡排序下标越界`
- ✅ `数组章节笔记：补复杂度表`
- ❌ `update` / `修改` / `1`

**提交节奏**：一节网课学完提交一次，不用每改一行就提交。

---

## 六、常见问题

**Q：Obsidian 里改了笔记，VS Code 里没看到？**
A：文件是同一份，VS Code 切回来会自动刷新；看不到就 `Ctrl+Shift+P` → 输入 `reload` 重新加载窗口。

**Q：不小心把 exe 之类的编译产物提交上去了？**
A：`.gitignore` 已经拦住了 `*.exe`、`x64/`、`Debug/` 等。若已提交，用 `git rm --cached 文件名` 移出（可以让 AI 帮你做）。

**Q：推送时报错要输入账号密码？**
A：第一次已经授权过，正常不会再有。若再弹窗，用 GitHub 账号登录一次并授权即可。

**Q：怎么确认真的推上去了？**
A：浏览器打开 https://github.com/HHHD823/algo-ds-learning ，看文件是否更新、提交历史里是否有新记录。
