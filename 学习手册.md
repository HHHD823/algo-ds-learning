# 学习手册（操作速查）

> 忘了怎么操作就回来翻这一页。在 Obsidian 左侧文件列表里点开即可。

## 一、三个软件的分工

| 软件 | 什么时候用 | 主要干什么 |
|------|-----------|-----------|
| Visual Studio | 听课跟练 | 敲代码、按 F5 编译运行、断点调试 |
| VS Code | 听课跟练 / 课下练习 | 敲代码、`Ctrl+Shift+B` 编译、`F5` 调试、`▶` 一键运行、提交推送 |
| Obsidian | 听课记笔记 | 编辑笔记、手动提交推送 |

三个软件打开的是**同一个文件夹** `D:\algo-ds-learning`，文件只有一份，不存在「同步」问题。

---

## 二、Obsidian 使用要点

### 基本界面
- **左侧**：文件列表（就是仓库目录，点文件夹图标展开）
- **中间**：编辑区，直接点开任意 `.md` 笔记就能写
- **右上角**：编辑 / 阅读模式切换，快捷键 `Ctrl+E`

### 必会快捷键

| 快捷键 | 作用 |
|--------|------|
| `Ctrl+E` | 在「编辑」和「阅读」模式之间切换 |
| `Ctrl+N` | 新建笔记 |
| `Ctrl+P` | 命令面板（调用所有功能，最重要） |
| `Ctrl+Shift+F` | 全仓库搜索（找「复杂度」这种词特别好用） |
| `[[` | 输入双方括号，链接到另一篇笔记 |
| `Ctrl+点击` | 跳转到链接指向的笔记 |

### 用模板新建章节笔记（一次性设置）

1. 设置（左下角齿轮）→ **核心插件** → 找到「模板」→ 开启
2. 设置 → 左侧「模板」选项卡 → **模板文件夹位置** 填 `_模板`
3. 以后新建笔记后，按 `Ctrl+P` → 输入「插入模板」→ 选 **章节笔记模板**

### 插入图片（必设项，共 3 处）

Obsidian → 设置 → **文件与链接**，把这三项都设对：

| 设置项 | 选什么 | 不设会怎样 |
|--------|--------|-----------|
| 内部链接类型 | **Markdown 链接** | 用 Wiki 链接的话，GitHub 不识别 |
| 新链接格式 | **相对于当前文件的路径** | 只写文件名，GitHub 上图显示不出来 |
| 新附件的默认位置 | **指定的附件文件夹** → 填 `附件` | 图片散落在仓库根目录，越用越乱 |

> ⚠️ 三项缺一个，图片在 GitHub 网页上就可能显示不出来。

粘贴图片：`Win+Shift+S` 截图 → 回到 Obsidian 按 `Ctrl+V`，图片自动存进 `附件/` 文件夹。

### 撤销 / 恢复误改的内容

Obsidian 是**实时保存**的，没有「保存」按钮。撤销分三种情况：

| 情况 | 怎么做 |
|------|--------|
| 刚改完、Obsidian 还开着 | `Ctrl+Z` 逐步撤销，`Ctrl+Y` 重做。界面**没有**撤销按钮，只能用快捷键 |
| 已经把 Obsidian 关了（撤销历史会清空） | `Ctrl+P` → 输入「文件恢复」→ 选带「打开」字样的那条 → 在时间轴里挑时间点恢复 |
| 改动之前提交过 Git | 最可靠。`Ctrl+P` → `Git: Open source control view` 查看改动，或让人用 git 帮你恢复到任意历史版本 |

> 💡 每完成一小节就 `Ctrl+P` → `Git: Commit and push`，任何误操作都能回退。

### 手动提交推送（不用等自动）

| 命令（`Ctrl+P` 后输入） | 作用 |
|------------------------|------|
| `Git: Commit` | 只在本地存档一次 |
| `Git: Push` | 推送到 GitHub |
| `Git: Commit and push` | 一步到位（最常用） |
| `Git: Pull` | 从 GitHub 拉取最新（在别处改过代码后用） |

> ⚠️ 提交前确认右下角有同步提示、没有报错。若报错「no changes」，说明没有改动需要提交。

---

## 三、在 Visual Studio 里新建章节代码（推荐做法）

### 做法 A：单文件模式（最简单，推荐新手）

不用建项目，仓库干净，目录结构和笔记并排：

1. 先在资源管理器（或 Obsidian 左侧）新建章节文件夹，如 `02-链表`
2. 打开 VS → 菜单 **文件 → 打开 → 文件** → 选中 `D:\algo-ds-learning\02-链表\linked_list.cpp`
   - 如果文件还不存在，先在文件夹里新建一个空的 `.txt` 再改名成 `.cpp`，或者用 VS Code 新建
3. 写代码 → 按 `Ctrl+F5` 直接编译运行（VS 会自动处理编译配置）
4. 保存后文件就落在仓库里了

**文件名建议用英文**（`linked_list.cpp`、`array_basic.cpp`），中文文件名在命令行工具里容易出编码问题。

### 做法 B：项目模式（代码需要多个文件时）

1. VS → **创建新项目** → 选「空项目」→ 下一步
2. **项目名称**：英文名，如 `LinkedList`
3. **位置**：`D:\algo-ds-learning\02-链表`
4. ✅ 勾选 **「将解决方案和项目放在同一目录中」**（很关键，否则会多一层套娃目录）
5. 创建后右键「源文件」→ 添加 → 新建项 → C++ 文件
6. 编译产物（`x64/`、`Debug/`、`.vs/`）会被 `.gitignore` 自动忽略，不会传上 GitHub

---

## 四、在 VS Code 里写代码并运行（已配好，可直接用）

### 环境说明
- 编译器：gcc / g++ 16.1（MinGW-w64，已加入系统 PATH）
- 调试器：gdb 17.2
- 配置保存在仓库 `.vscode/` 里，换电脑也跟着走

### 日常用法

1. 左侧「资源管理器」→ 在目标文件夹上**右键 → 新建文件**，命名如 `array.c`、`linked_list.c`
2. 写代码，`Ctrl+S` 保存
3. **运行代码**，三种方式任选：
   - 点右上角 **▶ 三角按钮**（Code Runner）或 `Ctrl+Alt+N`：编译并运行，结果显示在下方面板
   - `Ctrl+Shift+B`：只编译，生成同名 `.exe`
   - `F5`：编译 + 启动调试，可打断点单步看变量（学数据结构最有用）
4. 提交推送：左侧第三个图标 **源代码管理** → 写提交信息 → **✓ 提交** → **同步更改**

### 多文件项目（如 main.c + ArrayList.c）

`Ctrl+Shift+B` → 选 **「编译本文件夹全部 C 文件（多文件项目）」**，会把当前文件夹所有 `.c` 一起编译。

### ⚠️ 用 C/C++ 写代码必须注意的事

- **代码文件名请用英文**（`array.c`、`linked_list.c`），别用中文名——中文名走命令行编译会因编码问题报「找不到文件」
- **笔记文件名（`.md`）用中文完全没问题**，只影响代码文件

---

## 五、提交信息怎么写

好习惯：一句话说清「完成了什么」，中文即可。

- ✅ `完成单链表的增删改查`
- ✅ `修复冒泡排序下标越界`
- ✅ `数组章节笔记：补复杂度表`
- ❌ `update` / `修改` / `1`

**提交节奏**：一节网课学完提交一次，不用每改一行就提交。

---

## 六、常见问题

**Q：Obsidian 里改了笔记，VS Code 里没看到？**
A：文件是同一份，VS Code 切回来会自动刷新；看不到就 `Ctrl+Shift+P` → 输入 `reload` 重新加载窗口。

**Q：`▶` 运行按钮报「gcc 不是内部或外部命令」？**
A：编译器的 PATH 是后来配置的，**完全关闭 VS Code 再重新打开**即可（已运行的窗口读的是旧环境变量）。

**Q：不小心把 exe 之类的编译产物提交上去了？**
A：`.gitignore` 已经拦住了 `*.exe`、`x64/`、`Debug/` 等。若已提交，用 `git rm --cached 文件名` 移出（可以让 AI 帮你做）。

**Q：推送时报错要输入账号密码？**
A：第一次已经授权过，正常不会再有。若再弹窗，用 GitHub 账号登录一次并授权即可。

**Q：怎么确认真的推上去了？**
A：浏览器打开 https://github.com/HHHD823/algo-ds-learning ，看文件是否更新、提交历史里是否有新记录。
